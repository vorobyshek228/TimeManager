
type Events = {
    startDate:string | null,
    endDate?:string | null,
    title:string,
    allDay:boolean,
    // start: Date,
    // end?: Date,
    startTime?: string | null,
    endTime?: string | null,
    id: string
}
export default Events